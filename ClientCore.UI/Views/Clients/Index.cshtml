@model IEnumerable<ClientCore.Business.DTOs.GetClientDTO>

@{
    ViewData["Title"] = "Clients";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Clients</h1>
        <a asp-action="Create" class="btn btn-primary">Create New</a>
    </div>
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }


    <div class="card mb-4">
        <div class="card-body">
            <form method="get" class="row g-3">
                <div class="col-md-6">
                    <input type="text" name="searchString" value="@ViewData["CurrentFiler"]" class="form-control" placeholder="Search clients..."/>
                </div>
                <div class="col-md-4">
                    <select name="clientFilter" class="form-select">
                        <option value="All Clients"></option>
                        @* @foreach (var client in ViewBag.Clients)
                        {
                            <option value="@client">@client</option>
                        } *@
                    </select>
                </div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-outline-primary w-100">Search</button>
                </div>
            </form>
        </div>
    </div>

     @if (Model.Any())
     {
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th class="text-left">Name</th>
                        <th class="text-left">Code</th>
                        <th class="text-center">#Contact</th>
                        <th class="text-left">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="text-left">@Html.DisplayFor(modelItem => item.Name)</td>
                            <td class="text-left">@Html.DisplayFor(modeleItem => item.Code)</td>
                            <td class="text-center">@Html.DisplayFor(modelItem => item.TotalContacts)</td>
                            <td class="text-left">
                                <div class="btn-group" role="group">
                                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm">Details</a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
     }
     else{
        <div class="alert alert-info">
            No client(s) found.
        </div>
     }

</div>